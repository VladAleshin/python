
n = 30
total = 0
for num in range(2, n + 1):
    is_prime = True
    for j in range(2, num // 2 + 1):
        if num % j == 0:
            is_prime = False
            break
    if is_prime:
        total += num   

print(total)
